# TruLedgr API Environment Configuration

# Application Settings
APP_NAME="TruLedgr API"
DEBUG=true
HOST=127.0.0.1
PORT=8000

# Database Configuration
# Required: Set the database URL for your environment (async by default)
# For local development (SQLite with async support):
DATABASE_URL=sqlite+aiosqlite:///./truledgr.db

# For production (PostgreSQL with async support), replace with your connection string:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/truledgr

# For MySQL with async support:
# DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/truledgr

# Legacy sync URLs are automatically converted to async (not recommended):
# DATABASE_URL=sqlite:///./truledgr.db  -> becomes sqlite+aiosqlite:///./truledgr.db
# DATABASE_URL=postgresql://user:pass@host:5432/db -> becomes postgresql+asyncpg://user:pass@host:5432/db
